# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.9.2,<3.0.0
pydantic-settings>=2.10.1,<3.0.0  # Updated for langchain-community 0.4.x

# Database
psycopg[binary]==3.2.3  # Modern PostgreSQL adapter (psycopg3)
sqlalchemy==2.0.35
alembic==1.13.3

# Redis & Caching
redis==5.1.1
hiredis==3.0.0

# Background Tasks
celery[redis]==5.4.0  # Async task queue with Redis support
flower==2.0.1  # Celery monitoring UI

# HTTP Client
httpx==0.27.2
aiohttp==3.10.10
tenacity==9.0.0  # Retry logic with exponential backoff

# Browser Automation (LinkedIn + Web Scraping)
playwright==1.48.0  # Browserbase cloud browser automation

# Data Validation & Serialization
python-multipart==0.0.12
email-validator==2.2.0

# Authentication & Security
PyJWT==2.8.0  # JWT token management (preferred over python-jose in 2025)
pwdlib[bcrypt]==0.2.0  # Modern password hashing library (replaces passlib)
python-dotenv==1.0.1
authlib==1.3.0  # OAuth 2.0 client for HubSpot/LinkedIn integrations
cryptography==42.0.5  # Secure credential encryption (explicit version)
supabase==2.10.0  # Supabase client for authentication and database

# Monitoring & Logging
sentry-sdk[fastapi]==2.15.0
structlog==25.5.0  # Structured logging for application logs
# ddtrace==2.18.0  # Datadog APM (commented out - requires Rust toolchain)

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-httpx==0.33.0

# Development
black==24.10.0
flake8==7.1.1
mypy==1.11.2
pre-commit==4.0.1

# AI/ML - LangChain/LangGraph Framework (Updated Dec 2025 for v1.0)
langchain>=1.1.0,<2.0.0  # Core LangChain framework v1.0+
langchain-core>=1.2.0,<2.0.0  # LangChain core abstractions
langchain-community>=0.4.0,<1.0.0  # Community integrations
langgraph>=1.0.0,<2.0.0  # StateGraph orchestration for complex agents (v1.0+)
langgraph-checkpoint-redis>=0.3.0  # Redis checkpointer for LangGraph state persistence
langsmith>=0.4.0,<1.0.0  # LangSmith tracing and observability

# Deep Agents Implementation (Custom - 2025)
# Note: Using custom implementation based on LangChain patterns
# No external dependencies needed - built on existing LangChain/LangGraph

# LangChain Vector Store Integrations (Updated Dec 2025)
# langchain-postgres==0.0.12  # REMOVED: Conflicts with numpy>=2.1.0, unused in production
# langchain-community already included above - provides FAISS, Chroma, Qdrant
# langchain-huggingface==0.1.0  # REMOVED: Not used in production
langchain-ollama>=0.3.0  # Local models via Ollama (lang-core compatible)
langchain-text-splitters>=0.3.3  # Advanced text splitting strategies

# LangChain Integrations
langchain-anthropic>=1.3.0,<2.0.0  # Anthropic Claude integration (updated for lang-core compatibility)
langchain-openai>=1.0.0,<2.0.0  # OpenAI integration (updated for langchain-core 1.x)
# langchain-cerebras==0.5.0  # REMOVED: Requires Python <3.13, use cerebras-cloud-sdk directly

# AI/ML - Model Providers
cerebras-cloud-sdk==1.9.0  # Official Cerebras SDK (NEW: TCP warming, 21% faster than OpenAI wrapper)
openai>=1.58.1,<2.0.0  # For OpenAI-compatible APIs (RunPod/Cerebras via OpenAI client) - DOWNGRADED: langchain-openai 0.3.0 requires <2.0.0
anthropic>=0.75.0,<1.0.0  # Claude streaming and batch API support (updated for langchain-anthropic 1.3.0)

# Cloud Infrastructure - RunPod
# Note: Using RunPod for compute, not Firebase/GCP
# Firebase dependencies removed - will use RunPod API instead
runpod==1.7.6  # RunPod Python SDK for serverless and GPU compute
boto3==1.35.80  # AWS SDK for S3-compatible RunPod storage

# Document Processing
pdfplumber==0.11.4  # Best PDF text extraction (Context7 recommended)
python-docx==1.1.2  # DOCX document parsing
# pypandoc==1.13  # Universal document conversion (removed - not used)

# Export/Reporting (Task 11.5)
reportlab==4.2.5  # Professional PDF generation with charts and tables
openpyxl==3.1.5  # Excel file generation (.xlsx) with multi-sheet support

# Web Scraping
beautifulsoup4==4.12.3  # HTML parsing for static websites
playwright==1.48.0  # Browser automation for JavaScript-heavy sites (Browserbase integration)

# Social Media Scraping
tweepy==4.14.0  # Twitter API v2 client
praw==7.7.1  # Reddit API wrapper

# Vector Embeddings & Statistical Analysis
sentence-transformers==3.3.1  # For generating text embeddings
numpy>=2.1.0  # Required for vector operations (2.1+ has Python 3.13 support)
scipy>=1.14.0  # Statistical functions for A/B test analysis (1.14+ for Python 3.13 compatibility)
# pgvector==0.3.6  # REMOVED: Dependency of langchain-postgres, unused in production

# Web Search (for Research Agents - NEW: 2025)
tavily-python==0.7.12  # Production-grade web search API
duckduckgo-search==3.8.5  # Fallback search engine

# Voice Processing (Task 6: Cartesia Integration)
cartesia==2.0.13  # Cartesia AI SDK for TTS/STT (UPGRADED: WebSocket streaming, sonic-3, 50% faster)
# pyaudio==0.2.14  # REMOVED: Not imported anywhere in backend/app/, requires C headers (portaudio) that fail CI builds
# wave==0.0.2  # REMOVED: Not imported anywhere in backend/app/, Python stdlib has wave module if needed
# websockets==13.1  # REMOVED: Not imported anywhere in backend/app/, cartesia SDK handles WebSockets internally

# CRM Integrations (Task 5)
hubspot-api-client==9.0.0  # Official HubSpot CRM SDK
# Apollo.io: No official SDK - using httpx with API key authentication
# LinkedIn: Using authlib for OAuth 2.0 sign-in only (ToS-compliant)

# CLI Dependencies (Agent CLI)
rich==13.7.0  # Beautiful terminal UI for agent CLI
click==8.1.7  # CLI framework for command-line interface
typer==0.15.1  # Modern CLI framework built on Click with type hints

# Graph Database (Relationship Mapping - NEW: Nov 2025)
neo4j==5.26.0  # Official Neo4j Python driver for warm path discovery

# Lang-Core: Scientia Capital LangChain/LangGraph/LangSmith Stack (Dec 2025)
# Provides: LangChain middleware, LangSmith integration, LangGraph utilities,
#           multi-provider LLM selector, Celery tasks, FastAPI utilities, Redis utils
lang-core @ git+https://github.com/ScientiaCapital/lang-core.git
