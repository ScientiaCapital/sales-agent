---
phase: 01-pipeline-models
plan: 01
type: execute
---

<objective>
Add SQLAlchemy models for Close CRM Opportunities and Pipelines with database migration.

Purpose: Foundation for tracking deals through sales stages - all other Phase 1 work depends on these models.
Output: CloseOpportunity and ClosePipeline tables in database with proper indexes.
</objective>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@backend/app/models/crm.py
@backend/app/models/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CloseOpportunity SQLAlchemy model</name>
  <files>backend/app/models/crm.py</files>
  <action>
Add CloseOpportunity class following existing CRMContact pattern:
- external_id (String 255, unique, nullable=False) - Close opportunity ID
- platform (String 50, default="close")
- close_lead_id (String 255, nullable=False) - Reference to Close lead
- contact_id (Integer, ForeignKey to crm_contacts.id, nullable=True)
- name (String 255, nullable=False)
- description (Text, nullable=True)
- amount (Float, nullable=True) - Deal value
- currency (String 3, default="USD")
- stage (String 50, nullable=False) - e.g., "prospecting", "proposal", "won"
- probability (Float, nullable=True) - 0-100 win chance
- expected_close_date (DateTime, nullable=True)
- actual_close_date (DateTime, nullable=True)
- owner_id (String 255, nullable=True) - Close user ID
- last_synced_at (DateTime, nullable=True)
- sync_status (String 50, default="active")
- raw_data (JSON, nullable=True)
- created_at, updated_at timestamps

Add indexes:
- idx_opp_external_id (unique)
- idx_opp_close_lead_id
- idx_opp_stage_expected_close (stage, expected_close_date)
- idx_opp_sync_status

Use __tablename__ = "crm_opportunities"
  </action>
  <verify>Python syntax valid: python -c "from backend.app.models.crm import CloseOpportunity"</verify>
  <done>CloseOpportunity class exists with all fields and indexes defined</done>
</task>

<task type="auto">
  <name>Task 2: Add ClosePipeline SQLAlchemy model</name>
  <files>backend/app/models/crm.py</files>
  <action>
Add ClosePipeline class:
- external_id (String 255, unique, nullable=False) - Close pipeline ID
- platform (String 50, default="close")
- name (String 255, nullable=False)
- description (Text, nullable=True)
- stages_json (JSON, nullable=False) - Array of {name, order, default_probability}
- is_active (Boolean, default=True)
- created_by (String 255, nullable=True)
- created_at, updated_at timestamps

Add indexes:
- idx_pipeline_external_id (unique)
- idx_pipeline_active (is_active)
- idx_pipeline_name

Use __tablename__ = "crm_pipelines"
  </action>
  <verify>Python syntax valid: python -c "from backend.app.models.crm import ClosePipeline"</verify>
  <done>ClosePipeline class exists with all fields and indexes defined</done>
</task>

<task type="auto">
  <name>Task 3: Create Alembic migration</name>
  <files>backend/alembic/versions/xxxx_add_close_opportunities_pipelines.py</files>
  <action>
Generate migration:
cd backend && alembic revision --autogenerate -m "add_close_opportunities_pipelines"

Review generated migration to ensure:
- crm_opportunities table created with all columns
- crm_pipelines table created with all columns
- All indexes created
- Foreign key to crm_contacts.id included
- Downgrade properly drops tables

Run migration:
cd backend && alembic upgrade head
  </action>
  <verify>cd backend && alembic current shows latest migration applied</verify>
  <done>Migration runs successfully, tables visible in database</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CloseOpportunity model defined with all fields
- [ ] ClosePipeline model defined with all fields
- [ ] Alembic migration generated and applied
- [ ] Tables visible in database: crm_opportunities, crm_pipelines
- [ ] No Python import errors
</verification>

<success_criteria>
- All 3 tasks completed
- Both models importable from backend.app.models.crm
- Database has crm_opportunities and crm_pipelines tables
- No errors in migration
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-models/01-01-SUMMARY.md`
</output>
