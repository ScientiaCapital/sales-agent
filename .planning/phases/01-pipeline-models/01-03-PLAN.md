---
phase: 01-pipeline-models
plan: 03
type: execute
---

<objective>
Create FastAPI endpoints for Close CRM opportunity and pipeline management.

Purpose: Expose pipeline/opportunity data to the frontend and other services.
Output: New API router with CRUD endpoints mounted at /api/close.
</objective>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-models/01-01-SUMMARY.md
@.planning/phases/01-pipeline-models/01-02-SUMMARY.md
@backend/app/api/close_outreach.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create close_opportunities router</name>
  <files>backend/app/api/close_opportunities.py</files>
  <action>
Create new FastAPI router following close_outreach.py patterns:

```python
from fastapi import APIRouter, HTTPException, Depends
from pydantic import BaseModel, Field
from typing import Optional, List, Dict, Any
from datetime import datetime
import logging

from app.services.crm.close import CloseProvider
from app.services.crm.base import CRMNotFoundError, CRMValidationError, CRMRateLimitError
from app.core.config import settings

logger = logging.getLogger(__name__)
router = APIRouter(prefix="/close", tags=["close-opportunities"])
```

Add Pydantic request/response models:
- OpportunityCreateRequest(lead_id, name, amount?, pipeline_id?, expected_close_date?)
- OpportunityUpdateRequest(name?, amount?, stage?, expected_close_date?)
- OpportunityResponse(external_id, lead_id, name, amount, stage, ...)
  </action>
  <verify>File exists with router and models defined</verify>
  <done>Router created with Pydantic models</done>
</task>

<task type="auto">
  <name>Task 2: Add opportunity CRUD endpoints</name>
  <files>backend/app/api/close_opportunities.py</files>
  <action>
Add endpoints:

GET /close/opportunities - List opportunities with optional lead_id filter
GET /close/opportunities/{opportunity_id} - Get single opportunity
POST /close/opportunities - Create opportunity (requires lead_id)
PUT /close/opportunities/{opportunity_id} - Update opportunity

Each endpoint:
- Uses CloseProvider(api_key=settings.CLOSE_API_KEY)
- Wraps calls in try/except for CRM exceptions
- Returns appropriate HTTP status codes:
  - 404 for CRMNotFoundError
  - 422 for CRMValidationError
  - 429 for CRMRateLimitError
  - 200/201 for success
  </action>
  <verify>All 4 endpoints defined with proper error handling</verify>
  <done>CRUD endpoints for opportunities implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add pipelines endpoint and mount router</name>
  <files>backend/app/api/close_opportunities.py, backend/app/main.py</files>
  <action>
Add pipeline endpoint to close_opportunities.py:
GET /close/pipelines - List all pipelines from Close CRM

Mount router in main.py:
1. Import: from app.api.close_opportunities import router as close_opportunities_router
2. Add: app.include_router(close_opportunities_router, prefix="/api")

This makes endpoints available at:
- /api/close/opportunities
- /api/close/opportunities/{id}
- /api/close/pipelines
  </action>
  <verify>cd backend && python -c "from app.main import app; print([r.path for r in app.routes if 'close' in r.path])"</verify>
  <done>Pipelines endpoint added, router mounted in main.py</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] close_opportunities.py exists with router
- [ ] Pydantic request/response models defined
- [ ] GET /opportunities endpoint works
- [ ] POST /opportunities endpoint works
- [ ] PUT /opportunities/{id} endpoint works
- [ ] GET /pipelines endpoint works
- [ ] Router mounted in main.py
- [ ] No import errors when starting server
</verification>

<success_criteria>
- All endpoints implemented
- Router mounted and accessible
- Error handling covers all CRM exceptions
- Phase 1 complete - ready for Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-models/01-03-SUMMARY.md`:

# Phase 1 Plan 3: API Endpoints Summary

**[Substantive one-liner]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `backend/app/api/close_opportunities.py` - New router
- `backend/app/main.py` - Added router mount

## Next Step
Phase 1 complete, ready for Phase 2 (Activity Sync)
</output>
